#include "utility.h"

using namespace std;

class Solution {
public:
    Solution() {}
    TreeNode* mergeTrees(TreeNode* t1, TreeNode* t2);
    TreeNode* mergeTreesRecursive(TreeNode* t1, TreeNode* t2);
};