#include <vector>

using namespace std;

class Solution {
    public:
        Solution() {}
        ~Solution() {}
        vector<int> sortedSquares(vector<int>& A);
        vector<int> sortedSquares2(vector<int>& A);
};